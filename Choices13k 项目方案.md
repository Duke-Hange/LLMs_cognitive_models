# Choices13k 风险决策认知建模项目方案

本文档是当前项目根目录下唯一的总方案说明。它回答三个问题：为什么做这个研究、要回答什么科学问题、以及当前代码如何支撑研究设计。实验实现细节见 `experiments/README.md` 与各阶段 README。

## 1. 研究背景

Choices13k 是一个大规模“描述性风险决策”数据集。每一行任务给出两个选项 A/B，每个选项由若干结果金额与发生概率组成；人的群体选择结果通常以 `bRate` 表示，即选择 B 的比例。这个数据集适合用来研究一个经典问题：人在风险条件下的选择，究竟更接近可解释的符号规则、由数据拟合出来的神经函数，还是能够利用语言与预训练知识的大语言模型。

传统认知建模通常依赖期望值、期望效用、前景理论等符号模型。这类模型参数少、可解释性强，能够直接对应风险态度、概率加权、损失厌恶等心理学概念，但表达能力有限。神经网络模型则可以学习更复杂的非线性映射，可能在预测上更强，但其心理解释需要额外分析。LLM 模型引入了第三种可能：模型并不是只从本实验训练样本中学习，而是把风险、概率、金额、偏好等语言知识带入任务，再通过上下文示例或微调适配当前数据。

因此，本项目不是单纯追求最低误差，而是比较三类模型在同一任务、同一划分、同一指标下的表现差异，并把这些差异解释为不同认知假设的证据。

## 2. 科学问题

本项目当前围绕以下科学问题组织：

1. 符号模型与神经模型在样本效率上是否存在系统差异？
   符号模型具有强先验，少量样本下可能更稳定；神经模型容量更高，但可能需要更多训练样本才能超过符号模型。

2. LLM 的上下文学习是否能作为一种不同于传统训练的认知模型？
   ICL 条件下，LLM 不更新参数，只读取若干训练示例后预测测试任务。如果它在小样本下表现较好，说明语言预训练可能携带了与风险选择相关的结构性先验；如果表现不稳定，则提示语言理解并不等价于数值决策建模。

3. QLoRA 微调是否能把 LLM 从“提示驱动预测器”推进为“任务适配模型”？
   06 阶段用 05 的同一 `split_manifest` 训练文本回归模型。它的关键价值不是替代 03，而是检验参数更新后的 LLM 系列在相同样本规模下是否获得稳定收益。

4. 预测精度与心理解释之间如何取舍？
   符号模型可解释性最高，神经模型和 LLM 的预测能力可能更强。最终结论需要同时报告误差、学习曲线、置信区间、模型族差异和失败模式，而不能只看单个最优数值。

## 3. 核心研究思路

项目采用“统一数据、统一划分、统一指标、分阶段比较”的设计。

统一数据来自 `experiments/data/data.csv` 与 `experiments/03_LLM/results/prompts_en_v2.jsonl`。`data.csv` 保存结构化赌博任务与 `bRate`；prompt JSONL 保存 LLM 可读文本，并保留与 CSV 行号对齐的 `row_index`。05/06 的主协议使用 `target_mode = one_minus_bRate`，即预测选择 A 的群体比例；LLM 原始输出通常是 `P_B`，在统一评估时会转换到同一目标空间。

统一划分由 05 阶段生成 `split_manifest.json`。它记录训练集、测试集、每个 `(N, seed)` 的样本子集、目标定义与 prompt 文件哈希。04、05、06 的比较结论应优先以这个 manifest 或同等协议为准，避免不同模型使用不同测试集造成伪差异。

统一指标包括 MAE、MSE、RMSE、cross entropy、R2、correlation，以及 LLM 的 parse success rate。主图以学习曲线为核心：横轴是训练样本数 N，纵轴是指标表现；多 seed 结果报告均值和不确定性。可视化使用色盲友好且适合论文图的配色，模型族之间有明确颜色区分。

## 4. 模型族与阶段分工

当前代码按 00-06 分层：

- 00 数据准备：生成与校验统一数据入口，维护 `experiments/data/data.csv` 的可用性。
- 01 符号模型：实现 EV、EU、PT3、PT5 等增强符号模型，支持单独训练与学习曲线。
- 02 神经模型：实现 value-based 与 context-dependent 系列神经网络，支持统一目标下的训练与学习曲线。
- 03 LLM：生成英文 prompt，通过 Ollama 调用本地 LLM 进行 zero-shot / k-shot / ICL 预测；当前机器未部署真实模型时可用 dry-run 验证流程。
- 04 一阶段比较：比较符号模型与神经模型，不包含 LLM；用于先确认两类传统模型的基线关系。
- 05 主比较框架：在统一协议下比较 baseline、symbolic、neural、LLM，并可顺路触发 06 的 QLoRA。
- 06 QLoRA 文本回归：读取 05 的 manifest，训练或 smoke 验证 LLM 微调模型，并把结果合并回 05 的曲线与图表。

## 5. 当前科学判断

从架构上看，项目的科学路线是合理的：它没有把不同模型放在不同数据口径下做简单排行榜，而是围绕样本效率和模型族差异组织学习曲线。04 先做符号与神经模型的受控比较，05 再加入 LLM，06 再进一步加入参数微调 LLM，这个递进顺序清楚。

当前最大的限制也需要如实写入论文或报告：

- 数据标签是群体聚合比例，不是个体 trial-level 选择，因此模型解释对象是“刺激到群体选择比例”的映射，而不是单个被试的动态决策过程。
- 当前开发机器没有本地 Ollama 模型，LLM 与 QLoRA 的真实效果需要在部署模型的机器上完成。
- LLM 的数值输出需要解析，解析失败率本身就是结果的一部分，不能被静默忽略。
- QLoRA 的结论依赖基座模型、显存、训练轮数与随机种子，不能用一次小规模 smoke 结果替代正式实验。

## 6. 推荐执行路径

本地代码连通性优先用 smoke：

```powershell
python experiments/05_program/src/runners/run_all.py --smoke --no-staging --run-qlora --qlora-smoke --qlora-sample-sizes 8 --qlora-seeds 42
```

正式传统模型与 LLM 比较：

```powershell
python experiments/05_program/src/runners/run_all.py --no-staging
```

正式加入 QLoRA：

```powershell
python experiments/05_program/src/runners/run_all.py --no-staging --run-qlora --qlora-sample-sizes 8 32 128 --qlora-seeds 43 44
```

当另一台机器部署了 Ollama 与 QLoRA 依赖后，建议先跑完整 05，再跑 05+06，最后检查 `outputs/run_*/metrics/curve_aggregated.csv`、`curve_aggregated_with_qlora.csv` 与 `outputs/run_*/figures/` 中的主图。

## 7. 结论解释原则

最终结论建议按以下顺序解释：

1. 先看常数基线是否被所有模型稳定超过。
2. 再看符号模型与神经模型在小 N 和大 N 下的相对优势。
3. 再看 LLM ICL 是否在相同 N 下形成独立收益，而不是只在解析成功样本上看似更好。
4. 最后看 QLoRA 是否在同一 split 与同一指标下超过 ICL，并报告训练成本与稳定性。

如果不同模型的最优点接近，应优先讨论置信区间、seed 稳定性、解析失败、可解释性和计算成本，而不是过度解读单个小数位差异。
