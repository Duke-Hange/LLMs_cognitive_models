# Choices13k — English prompt, compact v3 (ICL-friendly)
# Placeholders: {option_a_prose}, {option_b_prose}
# Canonical string is in build_prompt_en.py (TEMPLATE_EN_V3).
# Same Option A/B prose style as v2 for parser compatibility; shorter framing + output rules.

Many participants each faced the same choice: Option A vs Option B.

Q: Which option do you prefer?

Option A: {option_a_prose}

Option B: {option_b_prose}

Your task: estimate the fraction who chose Option B (vs A). One number in [0, 1].

Use only the lottery text. Do not report your own preference.

Output (strict): exactly one line P_B=0.6273 (decimal point; no %, no extra words or lines).
