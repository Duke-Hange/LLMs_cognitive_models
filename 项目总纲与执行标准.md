# 项目说明：不同模型作为认知模型的比较框架

---

## 一、项目核心问题

本项目的目标不是比较“哪个模型更好”，而是回答：

> **当不同形式的模型被作为认知模型时，我们应该如何理解它们的能力与局限？**

换句话说：

> **不同模型在什么条件下，能够（或不能）成为好的认知模型？**

---

## 二、什么是认知模型？

我们从两个核心维度来评价一个认知模型：

### 1. 行为拟合能力（Behavioral Adequacy）
- 能否在相同task stimulus下复现人类行为
- 不只是accuracy，还包括行为模式的一致性

---

### 2. 解释能力（Explanatory Adequacy）
- 是否能提供对认知过程的可解释说明

可能的解释层级包括：

- 参数层（symbolic model）
- 表征层（neural network）
- 语言/推理层（LLM，如CoT、logits等）

---

## 三、三类模型的本质区别

本项目关注三类模型：

---

### 1. Symbolic Model
- 人类设计的结构（强先验）

优点：
- 数据需求低
- 易解释
- 稳定

缺点：
- 表达能力有限
- 存在结构上限（bias ceiling）

---

### 2. Neural Network (NN)
- 数据驱动（弱先验）

优点：
- 表达能力强
- 可逼近复杂函数

缺点：
- 数据需求高
- 表征不稳定
- 难解释

---

### 3. Large Language Model (LLM)
- 预训练带来的强先验（来自人类语料）

优点：
- 初始能力强（non-zero baseline）
- 泛化能力强
- 任务接口灵活

缺点：
- 内部机制不透明
- 难以区分“学到的”和“原本就会的”

---

## 四、关键思想（非常重要）

> **模型可以表现很好，但原因可能完全不同。**

我们真正关心的是：

- 模型是否学会了任务？
- 还是依赖已有先验？
- 不同模型“学会”的含义是否一致？

---

## 五、LLM的特殊处理（重点）

对于LLM，必须区分两部分：

### 1. 训练前能力（Pretrained Baseline）
- zero-shot / few-shot
- 反映预训练先验

---

### 2. 任务学习能力（SFT / LoRA）
- 在当前task数据上的提升

---

👉 核心分析：

> 最终表现 = 预训练能力 + 任务学习增量

---

---

## 六、实验核心变量

### 1. 数据量（N）
- small N
- medium N
- large N

---

### 2. 模型类型
- symbolic
- NN
- LLM（baseline + SFT）

---

---

## 七、实验流程（必须严格按此执行）

---

### Step 0：定义任务

#### 要做什么：
- 明确task
- 确定输入输出格式
- 准备数据

#### 输出：
- task描述（简要）
- 数据规模

---

---

### Step 1：建立baseline

#### 要做什么：
- 计算随机表现（random baseline）

---

#### 输出图（Plot 1）：

- X轴：N（数据量）
- Y轴：性能指标（如MAE或accuracy）
- 一条水平线（random baseline）

---

---

### Step 2：Symbolic Model

#### 要做什么：
- 在不同N下拟合模型

---

#### 输出图（Plot 2）：

- X轴：N
- Y轴：性能
- 单条曲线

---

#### 预期形态：

- 小N表现较好
- 很快饱和

---

---

### Step 3：Neural Network

#### 要做什么：
- 从头训练NN
- 不同N

---

#### 输出图（Plot 3）：

- X轴：N
- Y轴：性能
- 最好有多次运行（均值+方差）

---

#### 预期形态：

- 小N接近随机
- 随N增加逐渐提升

---

#### 必须检查：

- 如果性能≈随机 → 一定有问题
- 必须画loss curve（loss vs epoch）

---

---

### Step 4：LLM Baseline

#### 要做什么：
- zero-shot
- （可选few-shot）

---

#### 输出：

- baseline performance（一个点或一条线）

---

---

### Step 5：LLM SFT / LoRA

#### 要做什么：
- 在不同N下finetune

---

#### 输出图（Plot 4）：

- X轴：N
- Y轴：性能

---

#### 预期形态：

- 起点较高（baseline）
- 有一定提升

---

---

### Step 6：总对比（最重要）

#### 要做什么：
- 所有模型画在同一图上

---

#### 输出图（Main Plot）：

- X轴：N（必要时用log scale）
- Y轴：性能
- 曲线：
  - symbolic
  - NN
  - LLM（baseline + SFT）

---

#### 注意（非常重要）：

- 如果range差异大 → 用log scale或分图
- 不允许“看不出变化”的图

---

---

## 八、结果解释（必须写）

每次实验必须回答：

1. 观察到什么趋势？
2. 哪个模型sample efficiency最高？
3. 哪个模型最终性能最高？
4. 模型是否收敛到类似水平？
5. 如果是，它们是否在做同一件事？

---

---

## 九、诊断（更重要）

必须回答：

- 为什么symbolic会饱和？
- 为什么NN需要更多数据？
- 为什么LLM起点高？

---

---

## 十、下一步

必须提出：

- 一个明确假设
- 一个可以验证的实验

---

---

## 十一、常见错误（必须避免）

❌ 只展示结果，不解释  
❌ 坐标轴设置不合理（导致看不出趋势）  
❌ metric没有baseline对照  
❌ NN没训练好就下结论  
❌ 使用AI生成代码但不理解  

---

---

## 十二、总结

> 本项目的目标不是“把模型跑通”，  
>  
> 而是：
>
> **理解不同模型在认知建模中的角色，以及它们在不同条件下如何表现与被解释。**
